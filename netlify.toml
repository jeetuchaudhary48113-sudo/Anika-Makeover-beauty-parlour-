# Netlify Configuration File
# This file automatically registers the correct build settings and redirects 
# for Vite, React, and single-page application router fallbacks when you deploy the app.

[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
